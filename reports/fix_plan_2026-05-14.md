# 🤖 تقرير التطوير اليومي — 2026-05-14
> وقت التحليل: 05:35 UTC
> الحالة العامة: WARNING ⚠️ — مشكلتان متوسطتان أُصلحتا + بيانات حقيقية غير متاحة بعد

---

## 📊 حالة البوت — آخر 24 ساعة

- الصفقات المنفّذة: غير متاح (logs على VPS — لم تُرفع بعد لـ GitHub)
- الإشارات المولّدة: غير متاح
- Win Rate: غير متاح — ينتظر أول رفع logs
- P&L اليوم: غير متاح
- Drawdown: غير متاح
- وقت التشغيل: ~48 ساعة منذ أول تشغيل (2026-05-12)
- آخر heartbeat: غير متاح (يُرسل على Telegram مباشرة)

> **ملاحظة:** البوت يعمل على VPS منفصل. ملفات الـ logs تُرفع تلقائياً كل 6 ساعات عبر `push_logs_to_github()`.
> المتوقع توفّر أول بيانات حقيقية في الرفع القادم. كل التحليل اليوم مبني على قراءة الكود مباشرة.

---

## 🚨 إصلاحات فورية — طُبّقت تلقائياً اليوم

### [1] رسائل البوت لا تُكتب في logs — 3 أماكن في main.py
- **الملفات:** `main.py` السطور 660, 745-746, 776
- **المشكلة:** ثلاث رسائل مهمة تستخدم `print()` بدل `log.info/warning()`، مما يعني:
  - "السوق مغلق (عطلة نهاية الأسبوع)" → تختفي عند الإغلاق في VPS بدون terminal
  - "خطأ في توليد رؤية الإيميل" → لا تُكتب في `errors_*.log`
  - "الفحص التالي بعد X دقيقة" → لا يظهر في `bot_*.log`
- **الكود القديم:**
  ```python
  print(f"⚠️ خطأ في توليد رؤية الإيميل: {vi_err}")         # سطر 660
  print(f"\n⏸️ السوق مغلق (عطلة نهاية الأسبوع)")            # سطر 745
  print(f"   ⏰ الفحص التالي بعد 60 دقيقة...")              # سطر 746
  print(f"\n⏰ الفحص التالي بعد {SCAN_INTERVAL_MINUTES} دقيقة...")  # سطر 776
  ```
- **الكود الجديد:**
  ```python
  log.warning(f"خطأ في توليد رؤية الإيميل: {vi_err}")
  log.info(f"⏸️ السوق مغلق (عطلة نهاية الأسبوع) — {time.strftime('%H:%M')}")
  log.info("الفحص التالي بعد 60 دقيقة")
  log.info(f"الفحص التالي بعد {SCAN_INTERVAL_MINUTES} دقيقة")
  ```
- **الحالة:** ✅ طُبّق تلقائياً

---

### [2] حد الصفقات اليومية غير مُفعَّل فعلياً — `rm.daily_trades` لا يُزاد
- **الملف:** `main.py` السطر 449 (داخل `run_bot()`)
- **المشكلة:** `RiskManager` يتتبع `daily_trades` لمنع فتح أكثر من 3 صفقات/يوم، لكن:
  1. `rm.record_trade(pnl)` مُصمَّم للصفقات المُغلقة (لا المفتوحة)
  2. بعد فتح صفقة ناجحة، لا يوجد `rm.daily_trades += 1` في أي مكان
  3. نتيجة: `self.daily_trades` يبقى صفراً → الفحص `daily_trades >= 3` لا يُفعَّل أبداً
  4. إضافي: `rm` يُنشأ من جديد في كل دورة (كل 15 دقيقة) داخل `run_bot()` → المنع لا يستمر بين الدورات
- **الكود المُضاف بعد `open_pairs.add(pair)`:**
  ```python
  rm.daily_trades += 1  # تفعيل حد الصفقات اليومية داخل نفس الدورة
  ```
- **الحالة:** ✅ طُبّق جزئياً — يمنع الآن فتح أكثر من 3 صفقات في نفس الدورة الـ 15 دقيقة
- **⚠️ تحذير:** الإصلاح الكامل يحتاج refactor (انظر "تطوير مجدول" أدناه)

---

## 📅 تطوير مجدول — نهاية الأسبوع (السبت/الأحد)

### [1] Refactor إدارة RiskManager — إصلاح كامل لحد الصفقات
- **الملفات:** `main.py` (دالة `run_bot()`)
- **المشكلة الجذرية:** `rm = RiskManager(...)` داخل `run_bot()` ينشئ instance جديد كل 15 دقيقة → `daily_trades` يُعاد للصفر كل دورة
- **الإصلاح الصحيح:** نقل `rm` للخارج + تمريره كـ parameter لـ `run_bot(rm)`
  ```python
  # في main loop (خارج run_bot)
  rm = RiskManager(balance=BALANCE, risk_percent=RISK_PERCENT)
  # ...
  run_bot(rm)  # تمرير نفس الـ instance
  ```
- **التأثير المتوقع:** حد الصفقات اليومية (3 صفقات/يوم) يعمل فعلياً
- **وقت التنفيذ المقدّر:** 25 دقيقة (مع اختبار)

### [2] إضافة "strategy" key لكل Signal Generator
- **الملفات:** `strategy/eurusd_signal.py`, `gbpusd_signal.py`, `xauusd_signal.py`, `london_signal.py`
- **المشكلة:** إشعار Telegram يعرض "Breakout" للجميع — يصعّب التمييز بين الأزواج
- **الإصلاح:** إضافة `"strategy": "EURUSD NY Breakout"` في dict كل إشارة
- **وقت التنفيذ المقدّر:** 10 دقائق

### [3] إصلاح دقة الإغلاق الجزئي
- **الملف:** `risk/risk_manager.py` السطر 247
- **المشكلة:** `round(lots * 0.5, 2)` يقرّب 0.015 → 0.02 (زيادة 33% من المطلوب)
- **الإصلاح:**
  ```python
  import math
  partial_lots = max(0.01, math.floor(lots * 0.5 * 100) / 100)
  ```
- **وقت التنفيذ المقدّر:** 5 دقائق

---

## 🔍 تحت المراقبة — يحتاج بيانات أكثر

- **الموضوع:** Heartbeat timezone — `_now_h` يستخدم local time بدل UTC
  - **الملاحظة:** `main.py:663` `_now_h = datetime.now().hour` → local time الـ VPS. يؤثر على دقة أوقات الـ heartbeat والـ log push. لو الـ VPS UTC، لا مشكلة.
  - **متى نقرر؟:** تأكد timezone الـ VPS (شغّل `date` أو `timedatectl` على VPS)

- **الموضوع:** أداء EURUSD الحقيقي أول 48 ساعة
  - **الملاحظة:** البوت يعمل منذ 2026-05-12 بعد إصلاح datetime bug. Max DD الـ backtest -11.79% أعلى من باقي الأزواج. نحتاج أول 30 صفقة للتقييم.
  - **متى نقرر؟:** بعد 30 صفقة

- **الموضوع:** GBPUSD — min_range=60 pips (backtest: 41 صفقة في سنتين)
  - **الملاحظة:** معدل 1.7 صفقة/شهر منخفض جداً. كثير من الأيام بدون إشارة.
  - **متى نقرر؟:** بعد مراقبة شهر كامل

- **الموضوع:** `open_positions_count` لا يُحدَّث داخل حلقة الإشارات
  - **الملاحظة:** `can_trade(len(open_positions))` يستخدم snapshot قبل الحلقة. لو EURUSD وGBPUSD يُشيران في نفس الدورة، كلاهما قد يُنفَّذ رغم `max_open_positions=1`. يُعالَج جزئياً بالإصلاح [2] أعلاه عبر `daily_trades`.
  - **متى نقرر؟:** عند refactor الـ `run_bot()` في نهاية الأسبوع

---

## 💡 اقتراحات تطوير الاستراتيجية
*(منظور خبير التداول)*

1. **EURUSD + GBPUSD — نافذة النيويورك (13:00-15:00 UTC):**
   إشارات الـ NY Breakout تعتمد على Range مبني من London (07:00-13:00 UTC). في أيام الأرقام الاقتصادية الأمريكية الكبيرة (NFP، CPI، FOMC)، الـ Range الأولي يتوسع فجأة → إشارات كاذبة محتملة. **اقتراح:** إضافة فلتر للأخبار عالية التأثير عبر `data/news_calendar.py` الموجود.

2. **XAUUSD — ADX=25 ثابت:**
   الذهب أكثر حساسية لتوقعات التضخم من حركة السعر الخالصة. في فترات التضخم المرتفع (2025-2026)، ADX=25 قد يكون متحفظاً جداً. **اقتراح:** تجربة ADX=20 في backtest لزيادة الفرص.

3. **تنويع نوافذ XAUUSD:**
   الذهب يُظهر حركات قوية خلال إعلانات البنوك المركزية (17:00-18:00 UTC). هذه النافذة خارج Kill Zones الحالية. **اقتراح:** إضافة نافذة إخبارية شرطية (عند وجود Fed event فقط).

---

## 📈 متابعة التعديلات السابقة

| التعديل | التاريخ | النتيجة |
|---------|---------|---------|
| إصلاح datetime index EURUSD/GBPUSD | 2026-05-12 | ⏳ ينتظر بيانات — 48 ساعة من التشغيل |
| signal['asia_high'] KeyError fix | 2026-05-12 | ✅ لا أخطاء KeyError في الكود |
| LondonSignalGenerator NameError | 2026-05-12 | ✅ /analyze يعمل صحيح |
| print() → logger في trade_monitor | 2026-05-12 | ✅ الأخطاء تُكتب في errors log |
| print() → log() في main.py (3 أماكن) | 2026-05-14 | ✅ طُبّق اليوم |
| rm.daily_trades++ بعد فتح صفقة | 2026-05-14 | ✅ طُبّق جزئياً اليوم |
| Break-Even SELL fix | 2026-05-01 v2.1.0 | ✅ `entry + offset` صحيح في السطر 161 |
| Kill Zones XAUUSD | 2026-05-04 v2.3.0 | ✅ لندن + NY AM فقط |

---

## 📋 ملاحظات المراقبة اليومية

### توقعات السوق اليوم (2026-05-14 — الخميس)
- الخميس عادةً يوم جيد لـ NY Breakout (EURUSD/GBPUSD) — حجم تداول مرتفع قبل الجمعة
- XAUUSD: ADX يحتاج مراقبة — بعد حركة قوية في الأسبوع الماضي، قد يدخل ranging
- USDJPY London Breakout (07:00-10:00 UTC): راقب مستوى الين مع تقارير BoJ

### خلاصة الحالة الفنية للكود
- الكود سليم بشكل عام بعد 5 أيام من الإصلاحات المتراكمة
- لا أخطاء حرجة تمنع التنفيذ
- المشاكل المتبقية كلها medium/low priority وتُعالَج بشكل تدريجي
